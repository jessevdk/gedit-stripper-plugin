A very simple plugin that removes spaces from a line which consists of only
spaces, when pressing Enter.

Install:
	* mkdir -p $HOME/.local/share/gedit/plugins
	* cp -r stripper stripper.plugin $HOME/.local/share/gedit/plugins/
